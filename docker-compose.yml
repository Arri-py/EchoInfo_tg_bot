version: "3.9"

services:
  bot:
    build: .
    container_name: echo-info-bot
    environment:
      - BOT_TOKEN=${BOT_TOKEN}
      - OWNER_ID=${OWNER_ID:-}
    restart: unless-stopped
    command: python bot.py
